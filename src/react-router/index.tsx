import React from 'react'
import { Route, withRouter } from 'react-router';
import { BrowserRouter as Router, Link, ReactDOM } from 'react-router-dom'


//export const BackButton = () => { };
let Routr: Router;
export { Routr, Route, withRouter, Link, ReactDOM }
